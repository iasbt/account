# 测试策略

> **评估对象**: Account System V1.8.6
> **版本**: V1.8.6
> **状态**: ⚪ **手工规划中**

## 1. 测试现状

| 维度 | 现状 (0-10) | 评级 | 简评 |
| :--- | :--- | :--- | :--- |
| **单元测试** | 0.5 | 🔴 很弱 | 依赖已安装（Vitest），当前无有效用例。 |
| **集成测试** | 0.5 | 🔴 很弱 | Supertest 已安装，但无接口测试。 |
| **端到端测试** | 0.0 | 🔴 无 | 未建立端到端测试。 |
| **覆盖率** | < 1% | 🔴 极低 | 当前无覆盖率报表。 |

## 2. 自动化建设目标

### 2.1 目标：Phase 3 准备

| 层级 | 工具选型 | 目标覆盖率 | 核心场景 |
| :--- | :--- | :--- | :--- |
| **单元测试** | **Vitest** | > 60% | 后端 Service、前端工具函数 |
| **集成测试** | **Supertest** | > 80% | Auth 与 Admin 关键接口 |
| **端到端测试** | **Playwright** | 关键流程 | 注册 → 登录 → 个人中心 → 退出 |
| **压力测试** | **k6** | 基线 | P99 < 500ms (100 VU) |

## 3. 实施计划

### 3.1 基础设施搭建

1.  **Backend**:
    *   已安装 `vitest`, `supertest`。
    *   补齐测试入口与数据库 Mock。
2.  **Frontend**:
    *   引入 `@testing-library/react` 与 `jsdom`。
    *   增补 `vitest.config.ts` 与测试基座。
3.  **CI/CD**:
    *   配置 GitHub Actions。
    *   每次 Push 触发单测与覆盖率。

### 3.2 测试用例清单

#### 后端 (Backend API)
*   **Auth**:
    *   `POST /api/auth/register`: 成功注册、重复邮箱、验证码错误。
    *   `POST /api/auth/login`: 成功登录、密码错误、用户不存在。
    *   `GET /api/auth/me`: 成功获取信息、未登录 401。
*   **Health**:
    *   `GET /api/health`: 返回 `status: ok` 和正确版本号。

#### 前端 (Frontend Components)
*   **Login Page**:
    *   输入框正常渲染。
    *   点击登录按钮触发 API 调用。
    *   错误提示正常显示。
*   **Register Page**:
    *   表单验证（必填项、邮箱格式）。

## 4. 脚本模板

### 4.1 后端接口测试（Supertest）

```typescript
import request from 'supertest';
import app from '../app'; // 需从 server.js 导出 app

describe('Auth API', () => {
  it('should return 400 for missing fields', async () => {
    const res = await request(app)
      .post('/api/auth/login')
      .send({});
    expect(res.status).toBe(400);
    expect(res.body.message).toBe('参数不完整');
  });
});
```

### 4.2 前端组件测试（React Testing Library）

```typescript
import { render, screen, fireEvent } from '@testing-library/react';
import LoginPage from './LoginPage';

test('renders login form', () => {
  render(<LoginPage />);
  expect(screen.getByLabelText(/邮箱/i)).toBeInTheDocument();
  expect(screen.getByRole('button', { name: /登录/i })).toBeInTheDocument();
});
```

## 5. 结论

**立即行动**:
1.  **Backend**: 落地第一个 Health Check 接口测试。
2.  **Frontend**: 落地 Login 页面渲染测试。
3.  **CI**: 接入 Actions 并输出覆盖率报表。

只有建立了基本的测试防护网，才能放心地进行 Phase 3 的大规模开发。
