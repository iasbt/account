# 数据库架构一致性修复报告 (Database Consistency Fix Report)

> **日期**: 2026-02-26
> **执行人**: Trae AI
> **状态**: ✅ 已修复 (Fixed)
> **版本**: V1.0

## 1. 背景与问题陈述 (Background)

在对服务器端现行代码与历史备份数据（V1.x）进行比对分析时，发现了严重的数据库架构冲突。这些冲突若不解决，将直接导致 V2.0 版本的 SSO（单点登录）功能无法运行，且旧用户数据无法通过新系统验证。

### 1.1 核心冲突
1.  **Schema 不匹配**:
    *   **V1.x (备份)**: `applications` 表使用 `code` 作为唯一标识，`redirect_url` 为单一字符串。
    *   **V2.0 (标准)**: `applications` 表要求 `app_id` 作为标识，`allowed_origins` 必须为数组（支持多域名），且必须包含 `secret` 用于签名。
2.  **有毒迁移脚本**:
    *   项目路径下的 `scripts/migrations/001_create_apps_table.sql` 包含过时的表定义（无 `secret` 字段），该脚本一旦执行，将锁定错误的数据库结构，导致 V2.0 代码报错。
3.  **数据孤岛**:
    *   V1.x 的用户密码哈希（bcrypt）存储在 `password_hash` 字段，而 V2.0 标准使用 `password` 字段。直接导入会导致认证失败。

## 2. 修复执行方案 (Execution Plan)

根据“消除冲突、保留数据、对齐标准”的原则，执行了以下修复操作：

### 2.1 清理与重置
*   **动作**: 删除了 `scripts/migrations/001_create_apps_table.sql`。
*   **原因**: 该文件是导致架构冲突的根源，属于废弃的实验性代码。
*   **动作**: 创建了 `scripts/migrations/001_init_schema_v2.sql`。
*   **内容**: 基于 `docs/04_部署运维/附件/database.sql`（唯一真理来源），定义了标准的 `users`, `legacy_users`, `applications` 表结构。

### 2.2 数据 ETL 迁移
编写并执行了自动化迁移脚本 `scripts/migrate_v1_to_v2.js`，完成了从 CSV 备份到 Postgres 数据库的转换：

1.  **应用迁移 (Applications ETL)**:
    *   映射: `code` -> `app_id`
    *   转换: `redirect_url` -> `allowed_origins` (自动提取 Origin)
    *   增强: 自动生成高强度 `secret` (32字节 Hex)
2.  **用户迁移 (Users ETL)**:
    *   双写策略: 同时写入 `legacy_users` (保留原始哈希用于兼容) 和 `users` (V2.0 主表)。
    *   字段映射: `password_hash` -> `password`。

## 3. 修复结果 (Results)

### 3.1 数据库状态
*   ✅ **Schema 验证通过**: 所有表结构已符合 V2.0 标准。
*   ✅ **数据完整性**: 成功导入了备份中的核心数据。

### 3.2 应用凭证 (New Credentials)
以下是根据备份数据自动生成的新凭证，请务必更新到对应的应用环境变量中：

| 应用名称 | App ID | 新 Secret (部分展示) | 允许的回调域名 |
| :--- | :--- | :--- | :--- |
| **Gallery** | `gallery` | `7c40cf...3d4fd` | `http://119.91.71.30:5173` |
| **Life OS** | `life_os` | `40ca3e...61a02` | `http://localhost:3000` |

*(完整 Secret 请查看服务器上的 `scripts/migration_secrets_report.txt`)*

### 3.3 用户数据
*   **迁移数量**: 6 名用户。
*   **关键账号**:
    *   `sso_user_f2f28ae8`
    *   `test_user_trae_e62beb41`
    *   `i_asbt`
*   **验证**: 所有用户均已保留原始密码哈希，可直接登录。

## 4. 后续操作建议 (Next Steps)

1.  **更新环境变量**:
    *   请将生成的 Secret 配置到 Gallery 和 Life OS 的 `.env` 文件中 (`SSO_SECRET`)。
2.  **重启服务**:
    *   执行 `docker-compose restart account-backend` 确保缓存刷新。
3.  **冒烟测试**:
    *   尝试使用旧账号登录 Gallery，验证 SSO 跳转是否正常。

---
*Generated by Trae AI Auto-Fix Engine*
