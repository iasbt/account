FROM node:20-alpine

WORKDIR /app

# Copy dependency definitions
COPY package*.json ./

# Install production dependencies
RUN npm install --omit=dev --legacy-peer-deps

# Copy source code (Full Copy to ensure nothing is missing)
COPY . .

# Expose port
EXPOSE 3000

# Start command
CMD ["node", "server.js"]
